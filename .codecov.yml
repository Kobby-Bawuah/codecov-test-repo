comment:
  layout: "header, files, footer"
  hide_project_coverage: false

ignore:
  - "*/migrations/*"

flags:
  django:
    paths:
      - apps/django
    carryforward: true
  dashboard:
    paths:
      - apps/dashboard
    carryforward: true
